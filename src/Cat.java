public class Cat {
    String catName = "Cat";
    float catWeight = 3.0f;
    int catAge = 0;
    int catHunger = 20; //0-100, где 0 — сильно хочется, а 100 — полное насыщение
    int catPlay = 30; //0-100, где 0 — сильно хочется, а 100 — полное насыщение
}
